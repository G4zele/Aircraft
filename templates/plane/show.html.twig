{% extends 'base.html.twig' %}

{% block title %}Plane{% endblock %}

{% block body %}
    <h1>Plane</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ plane.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ plane.Type }}</td>
            </tr>
            <tr>
                <th>BoardNumber</th>
                <td>{{ plane.boardNumber }}</td>
            </tr>
            <tr>
                <th>ReleaseDate</th>
                <td>{{ plane.releaseDate ? plane.releaseDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>ReleasePlace</th>
                <td>{{ plane.releasePlace }}</td>
            </tr>
            <tr>
                <th>FixDate</th>
                <td>{{ plane.fixDate ? plane.fixDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>FixPlace</th>
                <td>{{ plane.fixPlace }}</td>
            </tr>
            <tr>
                <th>ExploTime</th>
                <td>{{ plane.exploTime }}</td>
            </tr>
            <tr>
                <th>FixExploTime</th>
                <td>{{ plane.fixExploTime }}</td>
            </tr>
            <tr>
                <th>StartingExploTime</th>
                <td>{{ plane.startingExploTime }}</td>
            </tr>
            <tr>
                <th>FlyTime</th>
                <td>{{ plane.FlyTime }}</td>
            </tr>
            <tr>
                <th>SitDowns</th>
                <td>{{ plane.sitDowns }}</td>
            </tr>
        </tbody>
    </table>
    <table class="table">
        <tbody>
            <tr>
                <th>Kt</th>
                <td>{{ remdata.KT }}</td>
            </tr>
            <tr>
                <th>Py</th>
                <td>{{ remdata.Py }}</td>
            </tr>
            <tr>
                <th>Kd</th>
                <td>{{ remdata.Kd }}</td>
            </tr>
            <tr>
                <th>Kl</th>
                <td>{{ remdata.Kl }}</td>
            </tr>
        </tbody>
    </table>
    <a href="{{ path('plane_index') }}">back to list</a>

    <a href="{{ path('plane_edit', {'id': plane.id}) }}">edit</a>

    {{ include('plane/_delete_form.html.twig') }}
{% endblock %}
