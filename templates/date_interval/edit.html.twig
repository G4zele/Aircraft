{% extends 'base.html.twig' %}

{% block title %}Edit DateInterval{% endblock %}

{% block body %}
    <h1>Edit DateInterval</h1>

    {{ include('date_interval/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('date_interval_index') }}">back to list</a>

    {{ include('date_interval/_delete_form.html.twig') }}
{% endblock %}
